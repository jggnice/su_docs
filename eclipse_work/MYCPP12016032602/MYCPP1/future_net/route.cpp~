
#include "route.h"
#include "lib_record.h"
#include <stdio.h>

//你要完成的功能总入口
void search_route(char *topo[5000], int edge_num, char *demand)
{
	
    	unsigned short result[] = {2, 6, 3};//示例中的一个解
	int num_of_demand = 0;
	int i,j;
	int adjacent[600][8]={0};

i = 0;
while(demand[i]){i++;}num_of_demand = i;

//output NA
//if(edge_num > 0)return;
//output  is not NA

//    for (int i = 0; i < 3; i++)
//       record_result(result[i]);


puts("__my__");
for(i = 0;i < num_of_demand; i += 2)
printf("_%d_",demand[i]-48);
}
